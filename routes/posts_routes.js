const express = require('express');
const router = express.Router();
//const {getAllPosts, createPost, updatePost, deletePost} = require('../controllers/posts_controller');


module.exports = router;
